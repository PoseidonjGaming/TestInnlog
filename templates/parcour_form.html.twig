{% extends 'base.html.twig' %}
{% block title %}Ajouter un parcour{% endblock %}

{% block body %}




<div class='row'>
<div class="col-md-4">
</div>
    <div class="col-md-6"><h1>Ajouter un nouveau parcour</h1>
        <div class="card mb-4" style="width: 30rem;">
            <div class="card-body">
                <div class="font">
                    <div class="row">
                        <div class="col mb-6">
                            {{form_start(form_parcour)}}
                            <p>Heure de début: {{form_widget(form_parcour.heureDebut)}}</p>
                            <p>Commentaire: {{form_widget(form_parcour.commentaire)}}</p>
                            <p>Disctance: {{form_widget(form_parcour.distance)}}</p>
                            <p>Type de sortie: {{form_widget(form_parcour.TypeSortie)}}</p>
                            <p>{{form_widget(form_parcour.Valider)}} <a class="btn btn-primary" href="{{ path('menu')}}" role="button">Retour</a></p>
                            
                            {{form_end(form_parcour)}}
                            
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}